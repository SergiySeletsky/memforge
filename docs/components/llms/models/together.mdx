---
title: Together
---

To use Together AI LLM models, set the `TOGETHER_API_KEY` environment variable. Get your API key from the [Together AI account settings](https://api.together.xyz/settings/api-keys).

## Usage

```ts
import { Memory } from "mem0ai/oss";

const memory = new Memory({
  llm: {
    provider: "together",
    config: {
      apiKey: process.env.TOGETHER_API_KEY,
      model: "mistralai/Mixtral-8x7B-Instruct-v0.1",
      temperature: 0.2,
      maxTokens: 2000,
    },
  },
});

const messages = [
  { role: "user", content: "I'm planning to watch a movie tonight. Any recommendations?" },
  { role: "assistant", content: "How about thriller movies? They can be quite engaging." },
  { role: "user", content: "I'm not a big fan of thriller movies but I love sci-fi movies." },
  { role: "assistant", content: "Got it! I'll avoid thriller recommendations and suggest sci-fi movies in the future." },
];

await memory.add(messages, { userId: "alice", metadata: { category: "movies" } });
```

## Config

All available parameters for the `together` config are present in [Master List of All Params in Config](../config).
