services:
  memforge_store:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      - POSTGRES_DB=memforge
      - POSTGRES_USER=memforge
      - POSTGRES_PASSWORD=memforge
    ports:
      - "5432:5432"
    volumes:
      - memforge_storage:/var/lib/postgresql/data